<%- include("partials/header") %>

    <section class="page-intro">
        <h1>
            <%= title %>
        </h1>
        <p>
            Browse the next five upcoming service projects and discover ways to get involved.
        </p>
    </section>

    <section class="list-section">
        <% if (projects && projects.length> 0) { %>
            <% projects.forEach(project=> { %>
                <article class="list-item">
                    <h2>
                        <a href="/project/<%= project.project_id %>">
                            <%= project.project_title %>
                        </a>
                    </h2>

                    <p>
                        <strong>Date:</strong>
                        <%= new Date(project.project_date).toLocaleDateString("en-US", { year: "numeric" , month: "long"
                            , day: "numeric" }) %>
                    </p>

                    <p>
                        <%= project.project_description %>
                    </p>

                    <p><strong>Location:</strong>
                        <%= project.project_location %>
                    </p>

                    <p>
                        <strong>Organization:</strong>
                        <a href="/organization/<%= project.organization_id %>">
                            <%= project.organization_name %>
                        </a>
                    </p>
                </article>
                <% }) %>
                    <% } else { %>
                        <article class="list-item">
                            <h2>No upcoming projects found</h2>
                            <p>Please check your database connection and project data.</p>
                        </article>
                        <% } %>
    </section>

    <%- include("partials/footer") %>
